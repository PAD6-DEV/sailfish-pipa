# Mainline SM8250: no android_usb; charger node name varies — usb-moded
# will also try to auto-score power_supply devices. Keep an explicit
# override file empty so merge does not force a missing path.
# Cable-less bring-up uses USB_MODED_ARGS=-r instead.
