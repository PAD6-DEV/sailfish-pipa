# Until linux-pipa builds these in (=y), load as early as systemd allows.
# DRM_MSM is built-in and probes before modules-load; deferred probe may
# still recover if these land within CONFIG_DRIVER_DEFERRED_PROBE_TIMEOUT.
panel-novatek-nt36532
ktz8866
