# Xiaomi Pad 6 — sensors live on Qualcomm SSC (SDSP), not AP IIO.
# hexagonrpcd-sdsp + firmware registry + /mnt/vendor/persist are prerequisites.
# Do NOT force iiosensorsadaptor until SSC is exposed as IIO (or a sensorfw
# SSC/libssc adaptor exists). Default adaptors stay; loading will fail cleanly
# until a real backend is available.
[plugins]
