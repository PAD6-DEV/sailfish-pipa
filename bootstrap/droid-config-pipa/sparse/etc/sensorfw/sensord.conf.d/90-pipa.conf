# Xiaomi Pad 6 — Qualcomm SSC via libssc (sensorfw-qt5-libssc).
# Requires hexagonrpcd-sdsp + /mnt/vendor/persist (see mnt-vendor-persist.mount).
[available]
# Force-enable even if ssu-sysinfo Feature_* caching is stale after first boot.
alssensor=True
accelerometersensor=True
orientationsensor=True
