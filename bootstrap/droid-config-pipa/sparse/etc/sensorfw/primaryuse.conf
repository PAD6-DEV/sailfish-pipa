[plugins]
accelerometeradaptor = sscaccelerometeradaptor
alsadaptor = sscalsadaptor

[accelerometer]
# Sailfish orientation was inverted with Plasma's -I mount matrix.
# Identity maps SSC accel → sensorfw axes correctly on pipa.
transformation_matrix = "1,0,0,0,1,0,0,0,1"
